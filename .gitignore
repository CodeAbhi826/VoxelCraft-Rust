# dependencies
node_modules

# next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# production
/build

# misc
.DS_Store
*.pem

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log
dev.log
dev.out.log
server.log

# env files (local config - never commit)
.env*

# vercel
.vercel

# local platform artifacts
local-*
.claude
.z-ai-config
skills/
prompt

# sandbox operational state
.zscripts/dev.pid
.zscripts/*.log

# database (runtime state)
db/
*.db

# Rust / VoxelCraft build artifacts
voxelcraft/target/
voxelcraft/target-release/
voxelcraft/dist-native/

# local deliverables folder (this repo IS the source)
/download/

# sandbox session scripts & auth state (not part of the project)
/scripts/

# snapshot
initial_snapshot.json
worklog.md

# user uploads & tool outputs (session artifacts, not project source)
/upload/
/tool-results/
voxelcraft/wasm-out/

# voxelcraft build outputs (scripts/build-all.sh)
dist/
stage/

# local headless-testing environment shims (mesa/lavapipe, xkb, ALSA stubs —
# downloaded for this sandbox only, never shipped)
voxelcraft/scripts/*.deb
voxelcraft/scripts/*-dir.html
voxelcraft/scripts/dirlist*.html
voxelcraft/scripts/di*.html
voxelcraft/scripts/lib-shim/
voxelcraft/scripts/mesa-vk/
voxelcraft/scripts/pc-shim/
voxelcraft/scripts/vk-icd/
voxelcraft/scripts/xkb-extract/
voxelcraft/scripts/wl-dir.html

# local test-run worlds (created by --smoke / manual runs)
voxelcraft/saves/
voxelcraft/scripts/xml2.html
voxelcraft/scripts/xml2b.html

# local first-run artifacts (created by running the game from a dev checkout)
voxelcraft/options.txt
voxelcraft/logs/
voxelcraft/resourcepacks/*
!voxelcraft/resourcepacks/.gitkeep
!voxelcraft/resourcepacks/README.md
# 2026-09-20: user-dropped shader packs never enter the repo (only the
# folder README + gitkeep ship — the legal rule: third-party shader
# packs are user downloads, never redistributed by us)
voxelcraft/shader-packs/*
!voxelcraft/shader-packs/.gitkeep
!voxelcraft/shader-packs/README.md
/builtin-pack/
/downloaded_build/
/options.txt
/saves/
/screenshots/
# local lavapiipe/Xvfb smoke harness extracts (session tooling, not project code)
voxelcraft/scripts/llvm-extract/
voxelcraft/scripts/di-extract/
voxelcraft/scripts/xml-extract/
voxelcraft/scripts/wl-extract/
voxelcraft/scripts/x11xcb-extract/
crates/vc-render/target/

# local benchmark and test build artifacts
bench_run.json
build-artifact/
.agents/sentinel_*
.agents/teamwork_*

# ---- 2026-09-21 repo slim-down: build outputs are BUILT, never committed
# (171 auto-committed wasm rebuilds = 687MB of history bloat; the bundle
# is produced by voxelcraft/scripts/rebuild-web-bundle.sh locally or by
# the wasm-build workflow's artifacts — never git-tracked again)
public/voxelcraft.js
public/voxelcraft_bg.wasm
public/voxelcraft.d.ts
public/voxelcraft_bg.wasm.d.ts
public/voxelcraft-pack/
public/voxelcraft-pack-classic-art/
build-release/
dist/

# dev diagnostic artifacts (stay local, never in the repo)
diag-shots/
docs/screenshots/

# 2026-09-24: headless in-game browser-test campaign artifacts (57 screenshots)
ingame-test/
