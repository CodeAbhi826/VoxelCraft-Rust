// VoxelCraft library: pure voxel engine logic (no GPU, no window, no UI).
// This is fully testable headless. The binary (main.rs) adds the renderer + UI shell.

pub mod blocks;
pub mod world;
pub mod player;
pub mod logger;
