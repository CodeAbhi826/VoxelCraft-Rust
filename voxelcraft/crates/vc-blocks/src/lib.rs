//! vc-blocks — block registry & BlockState system (Master Spec §5):
//! 1.16.5-pattern state ids, per-block properties, sound families,
//! plus `tint` (grass/foliage/water biome color math, §18).

pub mod blocks;
pub mod tint;
